# Overmax v0.4.0 릴리즈 노트

> v0.3.3 이후 변경 사항 (v0.4.0)  
> 반영 기준 커밋: `0fa5656` (`v0.3.3` ~ `0fa5656`, 문서 커밋 제외 최종)

---

## 🎮 새로워진 점 및 개선 사항

### 🧠 1. 지능형 다차원 선곡 추천 (Smart Recommendation) 및 맞춤 권장 난이도 도입
* **[체감 변화]**: 오늘 나의 판정 흐름과 장기 실력, V-Archive 레이팅 컷라인을 입체적으로 분석하여 실력 향상과 레이팅 상승에 가장 효과적인 곡을 똑똑하게 추천해 줍니다. 또한 곡 목록을 둘러보아도 추천 레벨이 흔들리지 않고 일관되게 고정되며, V-Archive 연동 여부와 상관없이 앱을 켜자마자 내 실력에 꼭 맞는 권장 난이도를 바로 확인할 수 있습니다.
* **[개선 세부 요약]**:
  * **오늘의 컨디션 맞춤 추천 (세션 모멘텀)**: 오늘의 게임 플레이 판정 흐름이 좋을 때는 적정 난이도 이상의 상위 도전곡(`UP`)을 추천하고, 피로가 쌓여 점수가 주춤할 때는 안정적인 회복용 곡(`REST`)을 제안하여 무리 없는 최적의 플레이 리듬을 유도합니다.
  * **방치된 곡 재도전 (오랫동안 안 친 곡 우선 안내)**: 방금 플레이한 곡이 오버레이에 연속으로 추천되어 화면을 차지하는 현상을 방지하고, 과거에 플레이한 후 오랫동안 기록 갱신을 시도하지 않은 성적 개선 유력 곡(`TRY`)을 우선적으로 안내합니다.
  * **V-Archive Top-50 방어 및 돌파 지원**: Top 50 랭킹 컷라인에 걸쳐 있는 수성 추천곡(`DEF`)이나 조금만 기록을 개선해도 Top 50에 새로 진입할 수 있는 돌파 유력 곡(`TOP`)을 정밀하게 포착하여 추천합니다.
  * **미플레이 곡 스마트 발굴 (`CLR`)**: 내가 도전해볼 만한 적정 난이도 범위 내에서 아직 한 번도 플레이해보지 않은 신규 곡을 발굴하여 추천합니다.
  * **상황별 미니멀 뱃지 및 호버 툴팁**: 곡명이 가려지는 불편을 없애기 위해 특별한 사유가 있는 추천곡에만 18px 미니멀 뱃지를 부여하였으며, 마우스 커서를 올리면 `Top-50 컷라인 돌파 유력 후보`, `상승세 모멘텀에 맞춘 상위 도전곡` 등 상황별 상세 사유가 툴팁으로 친절하게 표시됩니다.
  * **일관된 인게임 공식 권장 난이도 안내**: 곡 목록 탐색 시 커서가 위치한 곡에 따라 추천 레벨이 요동치던 현상을 없애고, 플레이어의 실력 분포를 바탕으로 안정적인 권장 난이도(예: `🎯 권장 SC 13` / `🎯 Rec: 13`)를 SC 및 일반 패드 패턴(NM/HD/MX) 각각의 기준에 맞추어 분리 제공합니다.
  * **오프라인/V-Archive 미연동 상태 즉시 지원**: V-Archive 웹 연동을 하지 않았더라도 로컬에 저장된 기존 플레이 기록을 자체 분석하여 앱 기동 즉시 정확한 맞춤 추천 레벨과 선곡 추천을 제공합니다.
* 💡 **이용 팁**: 기존의 단순 달성률 오름차순 정렬 방식을 선호하시는 경우, `설정(⚙) > 스마트 추천` 옵션을 끄면 언제든지 클래식 추천 방식으로 즉시 전환할 수 있습니다.

### 🛍️ 2. 보유 DLC 팩 자동 감지 및 미보유 곡 추천 배제
* **[체감 변화]**: 내가 구매하지 않은 DLC 곡이 추천 목록에 떠서 선곡창에서 헛걸음하는 일이 완전히 사라집니다.
* **[개선 세부 요약]**:
  * 별도로 보유 DLC를 설정창에서 체크하거나 입력할 필요 없이, 플레이어의 로컬 플레이 기록과 V-Archive 동기화 기록을 분석하여 보유 중인 DLC 팩 목록을 자동으로 파악합니다.
  * 기본 수록곡(Respect/기본 팩)과 플레이 이력이 1곡이라도 확인된 DLC 팩의 곡들만을 추천 후보로 엄선하므로, 미구매 콜라보나 DLC 곡이 추천 목록에 노출되지 않습니다.

### 🧹 3. 미플레이 곡 점수 잔류 방지 및 과거 오기록 자동 정화
* **[체감 변화]**: 이전에 플레이했던 곡의 점수가 새로 선택한 미플레이 곡 화면에 잘못 남아있던 현상을 완전히 차단했습니다.
* **[개선 세부 요약]**: 
  * 화면 인식 캐시 구조를 개편하여 곡 목록을 넘길 때 이전 곡의 점수나 콤보 데이터가 다른 곡으로 넘어가지 않도록 즉시 분리·초기화합니다.
  * 과거에 잘못 인식되어 로컬 기록에 남아있던 유령 기록(Ghost Records)이 있더라도, 곡 목록 화면에서 미플레이(0.00%) 상태를 확인하는 즉시 과거 오기록을 로컬 데이터에서 자동으로 삭제하여 추천 목록과 오버레이가 항상 깨끗하고 정확하게 유지됩니다.

### 🔄 4. 곡 목록 탐색 중 로컬 기록 자동 동기화 (앱 내부)
* **[체감 변화]**: 게임 내 곡 목록을 둘러보는 것만으로도 화면에 표시된 내 최신 기록이 Overmax 내부에 즉시 반영되어 맞춤 추천 정확도가 높아집니다.
* **[개선 세부 요약]**: 곡 목록 화면에서 커서가 위치한 곡의 모드/난이도/점수가 안정적으로 인식되면, Overmax의 내부 로컬 데이터베이스에 자동으로 기록되어 추천 곡 목록과 난이도 탭 정보를 최신 상태로 갱신합니다.
* 💡 **안내**: 이는 **Overmax 앱 내부의 로컬 기록 동기화**이며, V-Archive 웹사이트로 기록이 자동 업로드되는 것은 아닙니다. V-Archive 기록 전송은 이전과 동일하게 플레이어께서 직접 전송 버튼을 눌러 등록하실 수 있습니다.

### ⚡ 5. 이벤트 기반 처리 도입으로 인게임 버벅임 완화
* **[체감 변화]**: 화면을 분석하고 기록을 저장하는 과정에서 발생할 수 있었던 미세한 끊김(Stuttering)을 줄였습니다.
* **[개선 세부 요약]**: 매 프레임마다 화면을 검사하여 저장하던 기존 방식을 개선하여, 결과창이나 선곡창에서 기록이 완전히 확정되는 순간에만 단 1회 저장하도록 이벤트 방식으로 전환함으로써 불필요한 시스템 부하를 소거했습니다.

### 🎯 6. 상단 헤더 및 하단 푸터 화면 정돈
* **[체감 변화]**: 오버레이 하단과 상단의 여백과 버튼 크기가 깔끔하게 정돈되어 시각적 안정감을 높였습니다.
* **[개선 세부 요약]**: 하단 푸터에 자리 잡고 있던 V-Archive 동기화 버튼을 상단 헤더 우측(`[⬆] [🔄] [⚙]`)으로 이전하여 버튼 크기와 정렬을 통일하고, 하단 푸터에는 난이도별 평균 달성률과 완주 현황을 직관적으로 배치했습니다.

### 🎚️ 7. 내 플레이 스타일에 맞춘 권장 난이도 기준 레이팅(Target Rate) 4단계 선택
* **[체감 변화]**: 클리어와 순회 위주로 즐길 것인지, 99% 이상의 정밀 판정작이나 퍼펙트(100%)를 노릴 것인지 내 목표에 따라 권장 난이도 기준을 자유롭게 조절할 수 있습니다.
* **[개선 세부 요약]**:
  * `설정(⚙) > 추천 설정`에서 권장 레벨의 기준이 되는 목표 정확도를 4단계(`97%`, `99%` [기본값], `99.5%`, `100%`) 중 원하는 버튼을 눌러 즉시 변경할 수 있습니다.
  * 목표치를 `97%`로 낮추면 조금 더 높은 난이도의 상위 도전곡 위주로 권장 레벨이 상승하고, `99.5%`나 `100%`로 높이면 안정적인 판정작에 적합하도록 정밀한 권장 레벨이 안내됩니다.

### 🪟 8. 앱 실행 시 오버레이 화면 번쩍임(Flashing) 현상 완전 해결
* **[체감 변화]**: Overmax를 처음 켤 때 화면에 잠깐 하얀 직사각형 창이 나타났다가 사라지던 현상이 완전히 사라져 더욱 깔끔하게 실행됩니다.
* **[개선 세부 요약]**: 앱 기동 시 윈도우가 화면에 노출되기 전에 백그라운드에서 즉시 투명화 및 숨김 처리를 완료하도록 초기화 흐름을 개선하여, 게임을 켜기 전까지는 오버레이 창이 바탕화면에 일절 나타나지 않습니다.

### 🖥️ 9. 설정창 및 보조 창 화면 전면 개편 (4개 전용 탭 & 깔끔한 카드형 디자인)
* **[체감 변화]**: 길게 나열되어 있던 설정 화면을 용도에 따라 4개의 전용 탭으로 분리하여 찾기 쉬워졌으며, 입력창과 슬라이더가 널찍하고 반듯하게 정돈되어 설정 변경이 훨씬 편리해졌습니다.
* **[개선 세부 요약]**:
  * **4대 전용 탭 구조화**: `일반(General)`, `추천(Recommendation)`, `V-Archive`, `고급(Advanced)` 4개 탭으로 재구성하여 필요한 설정을 한눈에 직관적으로 파악할 수 있습니다.
  * **V-Archive 연동 및 업로드 분리**: V-Archive 계정 연결 설정과 기록 업로드 기능을 명확히 분리하고, 창을 열 때 1회 자동으로 계정 상태를 확인하여 불필요한 중복 조작을 없앴습니다.
  * **원클릭 세그먼트 버튼**: 캡처 방식(자동/DXGI/GDI), 추천 모드(스마트/클래식), 목표 정확도(97%~100%), 크기(S~XL), 언어 등을 한 번의 클릭으로 깔끔하게 변경할 수 있습니다.
  * **전폭 입력창과 32px 수직 정렬**: V-Archive ID, account.txt, Provider URL 입력창을 시원한 전폭(Full-width)으로 확장하고 우측 버튼 및 슬라이더와의 높이와 수직 정렬을 32px로 칼같이 통일했습니다.
  * **RTL 슬라이더 개선**: 투명도 슬라이더의 수치 텍스트 폭을 안정적으로 고정하여 값을 조절할 때 슬라이더 바 위치가 흔들리지 않고 부드럽게 동작합니다.

### 🌏 10. 일본어(日本語) 인터페이스 지원 추가 (`6bde370`, `855208a`)
* **[체감 변화]**: 설정창 언어 선택에서 `日本語`를 고르면 오버레이와 모든 보조 창의 문구가 일본어로 표시되어, 일본어 환경에서도 불편 없이 Overmax를 사용할 수 있습니다.
* **[개선 세부 요약]**:
  * 설정창, 동기화 창, 업데이트 안내, 시스템 알림 등 화면에 표시되는 전체 문구(139개)를 일본어로 제공합니다.
  * 곡 패턴(`譜面`), 권장 난이도(`推奨`) 등 게임 용어는 일본 플레이어에게 익숙한 표기를 사용했습니다.
  * 일본어 글꼴은 Windows 시스템 폰트(Meiryo/MS Gothic)를 자동으로 활용하므로 별도 설정이 필요하지 않습니다.
* 💡 **이용 팁**: `설정(⚙) > 일반 > 言語`에서 언제든지 한국어/English/日本語를 전환할 수 있습니다.

### ⚡ 11. 곡 목록 탐색 반응성 대폭 개선 — 오버레이 깜빡임 소거 (`e8c7ab8`)
* **[체감 변화]**: 곡을 넘기다 멈출 때마다 오버레이가 훅 사라졌다가 2초 뒤에야 돌아오던 현상이 사라졌습니다. 이제 선곡 화면에서는 오버레이가 안정적으로 유지되고, 곡을 옮기면 새 곡 정보로 빠르게 갱신됩니다.
* **[개선 세부 요약]**:
  * 화면 인식 결과 반영 주기를 상황에 맞게 조절하여, 곡 목록 진입·전환 인식이 최대 3배 빨라졌습니다.
  * 스크롤 중 한 번의 순간 실패로 오버레이가 꺼지던 판정 구조를 수정하여, 정지 후 바로 안정적으로 복귀합니다.
  * 게임 화면 전환(인게임 진입 등) 시에는 기존과 동일하게 즉시 오버레이를 내려 화면 방해가 없습니다.

---

## 🛠️ 엔지니어링 & 내부 아키텍처 변경점

### 🧩 1. 4-Phase 지능형 추천 엔진 & 통계적 실력 분포 모델 ($\mathcal{N}(\mu, \sigma^2)$)
* **[레인별 독립 가중치 블렌딩 모델 및 SC/Pad 2-Track]**:
  * 종합 평가 점수 산출식:
    $$\text{FinalScore} = \text{BaseScore}(\Delta\text{Floor}) + w_{\text{top50}} \cdot S_{\text{top50}} + w_{\text{retry}} \cdot S_{\text{retry}} + w_{\text{flow}} \cdot S_{\text{flow}}$$
  * **Phase 1 (재도전 레인)**: 목표 rate(99.5%) 격차 $\times$ 14일 최근성 감쇠 램프($S_{\text{retry}}$) 결합.
  * **Phase 2 (Top-50 경계 레인)**: V-Archive 41~50위 수성 곡 및 컷라인 -2.0 이내 돌파 후보곡 선별 (`idx_varchive_top50` 복합 인덱스로 0.05ms 이하 조회).
  * **Phase 3 (세션 모멘텀 레인)**: 0~200점 만점 자체 Performance Rating($\text{PR} = \text{FloorBase} + \text{RateWeight}$) 체계 기반 동적 플로우 가중치 부여 (4시간 세션 만료 가드).
  * **Phase 4 (미니멀 사유 뱃지)**: `RecommendReason` ADT 기반 18px 미니멀 렌더링 파이프라인 구축.
  * **통계적 실력 프로필(`SkillProfile`)**: 버튼별 SC 및 일반(Pad) 실력 분포를 평균 $\mu$(주력 층)와 분산 $\sigma$(안정성)로 모델링하여, 커서 위치에 영향받지 않는 일관된 권장 레벨 라벨 도출 및 SC/Pad Cross-Track Fallback을 지원합니다.
  * **도메인 안전 게이팅(Domain Gating)**: 물리적 안전 난이도($\text{Floor} \le \mu - 0.8\sigma$) 구간에서만 `REST` 사유를 허용하고, 고난도 곡에서는 `TRY`로 안전하게 전환하여 사유 부여의 정합성을 보장합니다.
  * **로컬 Top-50 Fallback 파이프라인**: V-Archive 미연동 환경에서도 로컬 `records` 테이블과 Performance Rating을 결합하여 실시간으로 Top-50 요약을 산출(`RecordManager::get_top50_summary_with_fallback`)함으로써 $0.04\text{ms}$ 극저지연 즉시 추천을 제공합니다.

### 🎯 2. 이벤트 주도형 `VerifiedPlayEvent` 및 Data-First `RoiCache` 도메인 아키텍처
* **[도메인 이벤트 분리 및 Zero-Allocation 관측 캐시]**:
  * 메인 UI 렌더 루프의 매 프레임 DB 평가 폴링을 전면 제거하고, 5프레임 연속 안정화(Hysteresis) 시점에 단 1회 Copy/Zero-Allocation 값 객체를 방출하도록 이벤트 계층을 분리했습니다.
  * 관측 단위와 체크섬을 단일 `RoiCache`로 캡슐화하여 키 변경 시 즉시 무효화하고, `ResultModeDiffLatch`로 결과창 연출 깜빡임을 안전하게 보정하며, 플레이 상태를 `PatternRecord::Unplayed`와 `PatternRecord::Played`의 ADT로 명시적 모델링했습니다.

### 🚀 3. 템플릿 매칭 Zero-Allocation 및 u32 비트마스크/Popcount 최적화
* **[스택 기반 비트 패킹 및 CPU Popcount 연산 전환]**:
  * 글자당 20여 회 발생하던 동적 `Vec<u8>` 힙 할당을 스택 기반 `[u32; 32]` 비트마스크 패킹 및 CPU `count_ones()`(popcnt) 연산으로 전환하여 매칭 연산 속도를 10~20배 고속화하고 힙 할당을 0으로 줄였습니다.
  * `detect_score`의 `char → String → parse → u32` 중복 왕복을 정수 직접 누적으로 직결하고, 미사용 문자열 필드를 제거하여 파이프라인 전체의 무할당화를 달성했습니다.

### 🌐 4. 크레이트 경계 정돈 및 데이터 무결성/동시성 가드 강화
* **[네트워크/ROI 위치 단일화 및 SQLite WAL 모드]**:
  * V-Archive HTTP 통신을 `overmax_data`로, ROI 좌표 설정을 `overmax_engine`으로 이관하여 도메인 경계 완결성을 확보하고 워크스페이스 패키지 버전을 일원화(`version.workspace = true`)했습니다.
  * `RecordDB`에 SQLite WAL 모드, Busy Timeout(5초), 지수 백오프 재시도 가드를 적용하고, `settings.user.json` 쓰기 시 100ms 디바운스 및 임시 파일 교체를 통한 원자적(Atomic) 저장을 적용했습니다.

### 📜 5. 결과창 실제 플레이 이력 전용 `play_events` 테이블 및 45초 디바운스/In-Place 갱신
* **[선곡창 탐색 오염 격리 및 원자적 이력화]**:
  * 선곡 화면 휠 스크롤 시 발생하는 `records.updated_at` 갱신이 세션 모멘텀을 오염시키지 않도록 결과창 완주(`is_result_screen == true`) 전용 `play_events` 테이블을 신설했습니다.
  * 팝업 재진입 중복을 45초 디바운스로 차단하고, 결과창 MAX COMBO 연출 지연 감지 시 45초 윈도우 내 In-place `UPDATE`를 수행하여 완벽한 데이터 정합성을 보장합니다.

### ⚙️ 6. Zero-Cost Enum Strategy 패턴 및 모놀리식 모듈 분리
* **[무상태 다형성 및 메모리 오버헤드 0]**:
  * 1,000줄 이상의 거대한 `recommend.rs`를 `composite.rs`, `local.rs`, `scoring.rs`, `strategy.rs`, `types.rs`, `tests.rs`로 완전히 분리했습니다.
  * `RecommendStrategy` Enum(`Smart` vs `Classic`)을 정의하여 동적 디스패치 및 힙 할당 없이 컴파일 타임 최적화 기반으로 알고리즘 분리를 구현했습니다.
  * 로컬 및 V-Archive 기록에 존재하는 DLC 팩 목록을 `HashSet`으로 수집하여 미보유 DLC 곡의 불필요한 추천을 사전에 원천 필터링합니다.

### 🎯 7. 레이팅 기반 실질 환산 난이도(Effective Floor) 동적 역산 및 4단계 Target Rate 프리셋
* **[설정 연동 Effective Floor 역산 공식]**:
  * 설정 파일에 `recommend.target_rate` 필드(`97.0`, `99.0`, `99.5`, `100.0`) 및 스냅 정규화(`ALLOWED_TARGET_RATES`)를 도입했습니다.
  * Performance Rating으로부터 목표 정확도 기준 환산 층수($\text{Effective Floor}$)를 산출하는 수식을 구현하여 추천 컨텍스트 전체에 무할당(Zero-Allocation)으로 연동했습니다:
    $$\text{Effective Floor} = \left(\frac{\text{Rating}}{\text{RateRatio}(\text{Target Rate}) \times (200 / 15)}\right).\text{clamp}(1.0, 17.0)$$
  * 설정창 [UI] 탭에 단일 행(`ui.horizontal`) 58px 가로 세그먼트 버튼 UI를 배치하고 `t!` 매크로 기반 다국어(i18n)를 완성했습니다.

### 🪟 8. 오버레이 뷰포트 0ms 즉각 은닉 및 1x1 마이크로 뷰포트 초기화
* **[winit 초기화 시점 프레임 버퍼 노출 원천 소거]**:
  * `eframe::run_native`의 생성 클로저 진입 시점에 `init_overlay_window_immediate()`를 즉시 호출하여, `NativeApp::new`의 무거운 초기화 작업이 시작되기 전에 `setup_overlay_window`(`WS_EX_LAYERED`, `WS_POPUP`, `DwmExtendFrameIntoClientArea`) 및 `ShowWindow(hwnd, SW_HIDE)`를 0ms 시점에 실행합니다.
  * `native_options`의 메인 뷰포트 초기 크기를 `[1.0, 1.0]` + `with_visible(false)`로 설정하여 winit 윈도우 생성 시 불투명 흰색 프레임이 DWM에 노출되는 시간적 틈을 물리적으로 차단했습니다.
  * 초기 탐색된 `HWND`를 `PlatformState`에 즉시 캐시 주입하여 첫 렌더 틱의 중복 `EnumWindows` 호출을 생략했습니다.

### 🎨 9. 데스크톱 보조 창 전용 ODDS 디자인 시스템 분리 및 RTL 레이아웃/컨트롤 표준화
* **[인게임 HUD 테마와 데스크톱 다이얼로그 테마 완전 격리]**:
  * 인게임 240x160 오버레이 전용 컴팩트 테마(`overlay_theme.rs`)와 독립된 데스크톱 다이얼로그 전용 시스템(`dialog_theme.rs` - Overmax Desktop Dialog System, ODDS)을 신설하여 14px/12px 타이포그래피 계층, 카드 배경/외곽선(`BG_CARD`, `BG_CARD_STROKE`), 32px 표준 컨트롤 높이(`CONTROL_HEIGHT`)를 구축했습니다.
  * **2행 전폭 입력 폼 (`field_row` + `text_input_with_button`)**: 1행 라벨/설명, 2행 TextEdit + Action Button 구조를 통해 다국어 번역 길이 차이에 구애받지 않는 반응형 전폭 인풋 파이프라인을 구축했습니다.
  * **RTL 슬라이더 (`rtl_slider`) & 세그먼트 루프 표준화**: `egui`의 `right_to_left` 레이아웃에서 발생하는 `Slider` 내부 역전 현상을 `[44px 고정 수치 라벨] -> [슬라이더 바(show_value(false))]` 순차 렌더링으로 우아하게 해결하고, 세그먼트 버튼들을 표준 RTL 역순 루프로 일관되게 정돈했습니다.
  * **동기화 및 보조 뷰포트 정렬 일치**: 동기화 창(`sync_ui.rs`)의 Steam ID `TextEdit` 내부 패딩(`symmetric(8, 6)`)과 스캔 버튼 높이를 32px로 일치시키고, 디버그 창 닫기 진단 로그 스팸을 정리했습니다.
### 🌐 10. Zero-Cost 다국어(i18n) 시스템의 일본어(JA) 로케일 확장 (`6bde370`, `855208a`)
* **[`t_select!` 이중 규칙 매크로 및 점진적 폴백 아키텍처]**:
  * 기존 2분기(`Ko`/`En`) 룩업을 유지한 채 선택적 `Ja` 암을 갖는 3분기 규칙을 추가하고, 번역이 미정의된 키는 컴파일 타임에 영어 폴백으로 분기하는 이중 규칙 구조를 도입하여 신규 로케일 확장 시 전 키 일괄 작업 강요를 제거했습니다.
  * 전체 139개 키(정적 룩업 + 동적 포맷터)에 `Ja => ...` 암을 추가하여 런타임 문자열 할당 없이(`AtomicU8` 로케일 스위치 + 상수 리터럴 반환) Zero-Cost/Zero-Allocation 특성을 그대로 유지했습니다.
  * CJK 글리프는 기존 `install_cjk_fonts`의 Windows 시스템 폰트 폴백 체인(`meiryo.ttc`, `msgothic.ttc`)으로 무비용 커버됨을 검증하여 폰트 임베딩 없이 해결했습니다.

### 🎯 11. 씬 폴링 SleepHint 스케줄링 및 이탈 판정 정밀화 (`e8c7ab8`)
* **[파이프라인 SSOT 폴링 정책 (SleepHint)]**:
  * 워커가 fingerprint 부재 시 무조건 1초 슬립하던 구조를 제거하고, `DetectionPipeline`이 `SleepHint(Active/Relaxed)`를 산출해 워커가 매핑만 수행하는 SSOT 구조로 전환. Unknown 워밍업(<3초) 120ms / 장기 Unknown 1초로 가변 주기 운영.
  * `unknown_since` 타임라인의 소유자를 `process_frame_shared`로 일원화하여 모든 씬 진입 경로에서 일관된 판단 보장.
* **[is_leaving 연속 미스 요건]**:
  * 단발성 미스(스크롤 정착 프레임의 카테고리 띠 탈락)가 `second_ratio < first_ratio` 휴리스틱에서 이탈로 오판되어 `detecting()` 방출 → 오버레이 ~2초 블랙아웃 유발.
  * 최근 폴링 전체가 실패했을 때만 이탈로 인정하도록 강화. 실제 인게임 진입은 연속 실패이므로 즉시 OFF 유지(오표시 제약 불변).
* **[씬 미스 진단 계측]**:
  * 폴링 미스 시 거절 단계(Centroid Kernel/카테고리 띠/유사도 임계)와 참조 썸네일 대비 픽셀 diff를 5초 텔레메트리 스냅샷(`cg/band/minsim/maxdiff`)으로 집계하여 향후 임계값 튜닝 근거 확보.
  * `SceneFrameView` 값 객체 도입으로 `output()` 9개 위치 인자 수프 해소 및 `too_many_arguments` 억제 제거.

### 🏗️ 12. 복잡도 감소 리팩터링 및 아키텍처 핫스팟 책임 분리 (`6109799`, `fe761a1`, `9f3c33a`, `28a9d4e`, `f9776f1`)
* **[동기화 창(`sync_ui.rs`) 거대 함수 컴포넌트화 (`6109799`, `fe761a1`)]**:
  * 454줄 단일 함수 `render_sync`를 `steam_account_card`, `filter_card`(+`render_filter_form`), `candidates_header`, `sort_candidates` 전용 컴포넌트로 추출하고, `render_sync`는 ~60줄의 순수 오케스트레이터로 경량화하여 UI 렌더링 유지보수성을 대폭 향상했습니다.
* **[앱 초기화 채널 번들화 및 업로드 후처리 분리(`native_app.rs`) (`9f3c33a`)]**:
  * `SyncWorkerChannels` 팩토리를 도입하여 `NativeApp::new`의 7개 튜플 채널 선언 및 수동 연결을 캡슐화했습니다.
  * `spawn_upload`의 V-Archive 캐시 병합/Top-50 랭크 흐름을 `sync_varchive_cache_after_upload` 헬퍼로 분리하고, 오프라인 폴백 페이로드 생성을 `upload_fallback_payload`로 단일화했습니다.
* **[선곡 계열 씬 감지 게이트 체인 통합(`detection_pipeline.rs`) (`28a9d4e`)]**:
  * Freestyle과 OpenMatch 씬 인식의 60줄 복붙 체인($\text{ROI Crop} \rightarrow \text{Centroid Kernel Gate} \rightarrow \text{Category Band Solidity} \rightarrow \text{Jacket Matcher}$)을 `run_jacket_match_gate` 단일 체인으로 통합하여 래더매치(Ladder Match) 확장 대비 및 거절 단계 진단(`SceneMissDiag`)을 무비용 일원화했습니다.
* **[`RecordDB` 책임별 디렉터리 모듈 분할(`rust/overmax_data/src/store/record_db/`) (`f9776f1`)]**:
  * 1,586줄 단일 파일을 `mod.rs`(구조체, CRUD 코어, 커넥션 풀/재시도 가드, play_events), `schema.rs`(테이블 DDL, ensure_schema, JSON 캐시 이관), `queries.rs`(Top-50 요약/랭크, 최근 기록, 동기화 후보 SQL 쿼리), `sync.rs`(V-Archive API 레코드 병합)로 분할하여 SQLite DDL, 리포트, API 동기화 책임을 격리하고 public API를 100% 보존했습니다.


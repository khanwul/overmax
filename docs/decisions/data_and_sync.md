# Data Storage, Cache & Sync Decision Log

SQLite 로컬 저장소, V-Archive 동기화, 캐시 관리(`StartupCacheManager`), 메모리 인덱스 최적화, 추천 프로바이더 관련 주요 설계 결정의 배경과 이유를 기록한다.

---

## 📋 Decision History

| 날짜 | 결정 | 이유 | 참조 |
|:---|:---|:---|:---|
| 2026-07-13 | RecordKey 및 RecordValue의 overmax_core 이전 | 핵심 도메인 별칭인 RecordKey 및 RecordValue를 가장 하위인 overmax_core로 옮겨 의존성 정방향 상속 및 여러 크레이트 간 공유 실현 | [game_state.rs](../../rust/overmax_core/src/game_state.rs) / [lib.rs](../../rust/overmax_data/src/lib.rs) |
| 2026-07-16 | 단일 곡 조회 API 활용한 캐시 최적화 | 업로드 성공 시 전체 캐시 갱신 대신 단일 곡 조회 API(?title=song_id)를 호출하여 로컬 캐시 JSON에 머지함으로써 디스크 I/O 렉 방지 및 네트워크 비용 최적화 | [native_app.rs](../../rust/overmax_app/src/ui/native_app.rs) / [sync.rs](../../rust/overmax_data/src/community/sync.rs) / [varchive_upload.rs](../../rust/overmax_app/src/system/varchive_upload.rs) |
| 2026-07-16 | since 파라미터 활용한 캐시 증분 조회 최적화 | 시작 시 및 설정창 갱신 시 로컬 캐시의 최종 updatedAt을 파악하여 API에 since 파라미터로 넘겨주고 변경분만 받아와 머지(Merge) 처리하는 고효율 증분 동기화 적용 | [native_app.rs](../../rust/overmax_app/src/ui/native_app.rs) / [sync.rs](../../rust/overmax_data/src/community/sync.rs) / [varchive_upload.rs](../../rust/overmax_app/src/system/varchive_upload.rs) |
| 2026-07-16 | 시작 시 자동 갱신 옵션 제거 및 상시 활성화 | since 기반 초경량 증분 조회 기능이 안전하게 작동하므로, UI의 auto_refresh 옵션을 제거하고 앱 시작 시 무조건 자동 동기화(Sync)가 진행되도록 개선 | [settings.rs](../../rust/overmax_data/src/config/settings.rs) / [native_app.rs](../../rust/overmax_app/src/ui/native_app.rs) / [settings_ui.rs](../../rust/overmax_app/src/ui/settings_ui.rs) |
| 2026-07-16 | V-Archive 캐시 SQLite DB 내장화 및 생성 컬럼 최적화 | 기존 JSON 파일 캐시를 SQLite DB(varchive_records)로 통합 및 자동 마이그레이션 적용. score, max_combo, updated_at, rating을 생성형(STORED) 물리 컬럼으로 빼고 복합 인덱스를 적용해 렉 없는 O(1) 조회 성능 확보 | [record_db.rs](../../rust/overmax_data/src/store/record_db.rs) / [native_app.rs](../../rust/overmax_app/src/ui/native_app.rs) / [record_manager.rs](../../rust/overmax_data/src/service/record_manager.rs) |
| 2026-07-17 | `overmax_data` 곡 매칭 및 추천 분기 가드 정돈 | `client.rs` 및 `recommend.rs` 내의 2중 `if let` 중첩들을 `and_then` 모나딕 체인으로 정리하고, `split_once` 및 `let Some = ... else { continue }` 가드 패턴 도입 | [client.rs](../../rust/overmax_data/src/community/client.rs) / [recommend.rs](../../rust/overmax_data/src/service/recommend.rs) |
| 2026-07-21 | Sync Window 및 Quick Upload 분기를 위한 RecordKey/is_quick_upload 기반 이벤트 전환 | UI 인덱스(usize) 대신 RecordKey(song_id, mode, diff) 및 is_quick_upload 플래그를 채널로 전달하여, 비동기 스캔 후 인덱스 꼬임 방지 및 오버레이 Toast / Sync Window 카드 상태 변경 분기를 명확히 독립 보장 | [sync_ui.rs](../../rust/overmax_app/src/ui/sync_ui.rs) / [native_app.rs](../../rust/overmax_app/src/ui/native_app.rs) / [native_app_viewports.rs](../../rust/overmax_app/src/ui/native_app_viewports.rs) |
| 2026-07-21 | RecordDB 단일 SQL LEFT JOIN 쿼리 기반 동기화 후보 추출 구조 개편 | records와 varchive_records 테이블을 단일 LEFT JOIN 쿼리로 직접 대조하여 300여개 가짜 후보(False Positive) 노출 문제를 해결하고 Rust 이중 메모리 로드 로직 소거 | [record_db.rs](../../rust/overmax_data/src/store/record_db.rs) / [sync.rs](../../rust/overmax_data/src/community/sync.rs) |
| 2026-07-21 | RecordManager O(1) 증분 캐시 갱신(upsert_varchive_record) 도입 | 1곡 업로드 성공 시 전체 DB 재조회를 수행하는 무거운 refresh() 대신, 메모리 캐시 및 dirty 키만 O(1)로 증분 갱신하여 CPU/디스크 부하 대폭 절감 | [record_manager.rs](../../rust/overmax_data/src/service/record_manager.rs) / [native_app.rs](../../rust/overmax_app/src/ui/native_app.rs) |
| 2026-08-05 | 추천 시스템 Trait 추상화 및 Recommend Provider Protocol (v1) 구축 | 추천 곡 다양화를 위해 `RecommendationSource` trait 및 `CompositeRecommender`를 도입하고 백그라운드 HTTP fetcher (`recommend_provider_fetch.rs`) 및 slim protocol spec (`docs/guides/recommend-provider-protocol.md`) 구축 완료 | [recommend.rs](../../rust/overmax_data/src/service/recommend.rs) / [recommend_provider_fetch.rs](../../rust/overmax_app/src/system/recommend_provider_fetch.rs) / [recommend-provider-protocol.md](../guides/recommend-provider-protocol.md) |
| 2026-08-12 | VArchiveDB title_map을 인덱스 기반으로 전환 | 실측 결과 build_index()가 RSS를 1,940KB (88.5%) 증가시킴을 확인하고 `HashMap<String, Vec<usize>>`로 전환하여 100% 절감 | [client.rs](../../rust/overmax_data/src/community/client.rs) |
| 2026-08-12 | RecordDB get_rate_map 스레드 로컬 커넥션 캐싱 도입 | 매 호출 Connection::open으로 인한 읽기 지연시간(1.09ms)을 thread_local 커넥션 재사용으로 0.20ms(81.7% 감소)로 축소 | [record_db.rs](../../rust/overmax_data/src/store/record_db.rs) |
| 2026-08-15 | `StartupCacheManager` 캡슐화 모듈 도입 | 앱 기동 시 24시간 TTL 만료 캐시 다운로드로 인한 동기 메인 스레드 멈춤 현상을 100% 제거하고, 필수 캐시 부재 시 Cold Start(동기), 정상 존재 시 Warm Start(Non-blocking 비동기 + Arc Swap)로 캡슐화하여 0.1초 즉시 부팅 구현 | [cache_update.rs](../../rust/overmax_app/src/system/cache_update.rs) / [native_app.rs](../../rust/overmax_app/src/ui/native_app.rs) |
| 2026-08-18 | RecordDB SQLite WAL 모드 & busy_timeout 5000ms 강제 및 with_retry 가드 도입 | 다중 스레드 동시 쓰기(V-Archive 동기화 + 인게임 결과창 저장) 시 발생하는 `SQLITE_BUSY` 락 충돌 및 기록 유실을 방지하기 위해 WAL 모드 강제 및 지수 백오프 재시도 가드 적용 | [record_db.rs](../../rust/overmax_data/src/store/record_db.rs) |
| 2026-08-18 | SettingsDebounceWriter 및 원자적 파일 교체 도입 | UI 슬라이더 조작 시 매번 스레드를 띄우는 I/O 레이스를 방지하기 위해 100ms 디바운스 백그라운드 큐 구축 및 임시 파일 기반 atomic rename 적용 | [settings_writer.rs](../../rust/overmax_app/src/system/settings_writer.rs) / [settings.rs](../../rust/overmax_data/src/config/settings.rs) |
| 2026-08-18 | V-Archive 네트워크 I/O 및 외부 API 통신 overmax_data 일원화 | `overmax_app::system`에 흩어져 있던 HTTP I/O(`cache_update`, `varchive_upload`, `recommend_provider_fetch`)를 `overmax_data::community` 및 `overmax_data::service`로 통합하여 데이터 계층의 완결성을 확보하고 UI 앱 계층을 순수 렌더링/데스크톱 런타임으로 격리 | [cache_downloader.rs](../../rust/overmax_data/src/community/cache_downloader.rs) / [varchive_api.rs](../../rust/overmax_data/src/community/varchive_api.rs) / [recommend_provider_fetch.rs](../../rust/overmax_data/src/service/recommend_provider_fetch.rs) |
| 2026-08-21 | 추천 엔진 Phase 1(재도전 레인): rate 격차 × 최근성 가중 정렬 도입 | 단순 rate 오름차순 대신 `updated_at` 기반 최근성 가중치(12h 당일 유예, 14일 램프)와 목표 rate(99.5%) 격차를 곱한 `retry_priority`로 정렬하여 방금 친 곡의 당일 추천을 억제하고 방치된 곡의 재도전을 유인 | [recommend.rs](../../rust/overmax_data/src/service/recommend.rs) / [record_db.rs](../../rust/overmax_data/src/store/record_db.rs) / [2026-08-21-recommend-phase1-retry-lane-spec.md](../plans/2026-08-21-recommend-phase1-retry-lane-spec.md) |
| 2026-08-21 | 추천 엔진 Phase 2(Top-50 경계 레인): 컷라인 방어/돌파 가중치 블렌딩 도입 | V-Archive Top-50 합산 레이팅 체계에서 41~50위 수성 곡(3.0~5.0 보너스) 및 컷라인 -2.0 이내 돌파 곡(최대 6.0 보너스)을 선별하는 `top50_boundary_score`를 도입하고, `idx_varchive_top50` 복합 인덱스로 쿼리 지연시간을 0.05ms 이하로 최적화 | [recommend.rs](../../rust/overmax_data/src/service/recommend.rs) / [record_db.rs](../../rust/overmax_data/src/store/record_db.rs) / [2026-08-21-recommend-phase2-top50-boundary-spec.md](../plans/2026-08-21-recommend-phase2-top50-boundary-spec.md) |
| 2026-08-21 | 추천 엔진 Phase 3(세션 모멘텀 레인): 자체 Performance Rating 기반 동적 난이도 플로우 도입 | 단순 rate 대신 0~200점 만점 스케일의 자체 Performance Rating(`calculate_performance_rating`)을 도입하여 난이도별 점수 왜곡을 제거하고, 세션 평균 레이팅 대비 편차(+3.0점+, -6.0점~)로 상승 도전(+0.25)/안정화(동급)/회복(-0.3) 가중치를 부여하는 `session_flow_score` 및 4시간 만료 가드 구축 | [recommend.rs](../../rust/overmax_data/src/service/recommend.rs) / [record_db.rs](../../rust/overmax_data/src/store/record_db.rs) / [2026-08-21-recommend-phase3-session-flow-spec.md](../plans/2026-08-21-recommend-phase3-session-flow-spec.md) |
| 2026-08-21 | 결과창 실제 플레이 이력 전용 `play_events` 테이블 신설 및 45초 디바운스 가드 | 선곡창 휠 스크롤로 인한 `records.updated_at` 오염을 차단하기 위해 결과창(`is_result_screen == true`) 전용 Append-Only `play_events` 테이블을 신설하고, 레벨업/언락 팝업 재진입 중복을 45초 디바운스로 원천 방어하여 세션 모멘텀의 데이터 신뢰도를 100% 확보 | [record_db.rs](../../rust/overmax_data/src/store/record_db.rs) / [record_manager.rs](../../rust/overmax_data/src/service/record_manager.rs) / [2026-08-21-recommend-phase3-session-flow-spec.md](../plans/2026-08-21-recommend-phase3-session-flow-spec.md) |
| 2026-08-21 | Zero-Cost Enum Strategy 패턴 기반 추천 알고리즘 분리 (`RecommendStrategy`) | `LocalFloorRecommender` 내부의 정렬/평가/사유 부여/푸터 레벨 도출 로직을 무상태 `RecommendStrategy` (`Smart` vs `Classic`)로 분리하여, 캐시 복제 및 힙 할당/vtable 오버헤드 0의 완벽한 모듈성 및 신규 추천 알고리즘 확장성을 확보 | [recommend.rs](../../rust/overmax_data/src/service/recommend.rs) / [settings.rs](../../rust/overmax_data/src/config/settings.rs) |
| 2026-08-21 | 손풀기(Warm-up) 곡 세션 왜곡 방지 및 Peak Floor 앵커링 가드 도입 | 저난도 손풀기 곡(SC 6, 8 등)으로 인해 세션 평균 레이팅이 급락하거나 권장 레벨이 저난도로 곤두박질치는 현상을 방지하기 위해, Peak Rating 대비 30점 이상 낮은 곡을 세션 평균 집계에서 자동 제외하고 세션 내 최고/주력 난이도(Peak Floor)를 앵커로 유지하여 실력대 중심의 안정적인 추천 반응성을 구축 | [recommend.rs](../../rust/overmax_data/src/service/recommend.rs) |
| 2026-08-21 | 권장 레벨 Top 50 기반 앵커링 & 세션 데이터 점진적(Smooth) 블렌딩 개편 | 선곡창 탐색 시 커서 위치(`current_floor`)에 따라 권장 레벨이 요동치거나 손풀기 1판(SC 5 등)으로 권장 레벨이 급락하던 현상을 해결하기 위해, 커서 의존성을 100% 제거하고 V-Archive Top 50 장기 실력대를 기본 앵커로 고정하며 주력 세션 플레이 누적 수(1~4판)에 따라 세션 데이터를 점진적으로(25%~100%) 스근하게 블렌딩하도록 전면 개편 | [scoring.rs](../../rust/overmax_data/src/service/recommend/scoring.rs) / [strategy.rs](../../rust/overmax_data/src/service/recommend/strategy.rs) |
| 2026-08-21 | 권장 레벨 Top 50 중앙값(Median) 앵커링 및 세션 이력 `play_events` 100% 전용 격리 | 상위 15개 한계곡 평균 산출로 인해 권장 레벨이 과도하게 높게 잡히던 문제를 Top 50 중앙값(Median) 방식으로 개선하고, 선곡창 휠 스크롤로 오염된 `records` 테이블 fallback을 완전히 제거하여 실제 결과창 완주 로그(`play_events`)만으로 세션 이력을 순수 집계하도록 수정 | [scoring.rs](../../rust/overmax_data/src/service/recommend/scoring.rs) / [record_db.rs](../../rust/overmax_data/src/store/record_db.rs) |


